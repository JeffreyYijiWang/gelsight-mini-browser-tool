Build an online texture dictionary for my GelSight Material Studio project. Read the existing project, AGENTS.md, and GelSight_Material_Studio_Codex_Prompt.md if available. Integrate with the existing capture, atlas, material, and print workflows without replacing them. If the collection website needs a separate frontend, create it in a clearly named project folder and share the asset schema. Implement functioning ingestion, editing, public browsing, and a deployable production build; do not stop at a mockup.
Use gyotaku fish printing as an artistic reference for making an impression from a real surface. This is a digital interpretation: I collect a tactile scan, turn its relief into an ink-like print, and preserve it as a specimen in my personal texture dictionary. Do not present the application as a traditional gyotaku technique or reduce the reference to decorative fish illustrations.
The collection can include skin wrinkles, bark, leaves, textiles, stone, dried paint, manufactured surfaces, and other textures I actually capture. Each entry should feel like an individual collected impression with a title, accession number, scale, and collection story. Visitors should be able to explore the print, inspect its source geometry, rotate a material sphere, and see a printable relief when available.
Use the working title “Texture Dictionary” and subtitle “A collection of surfaces, recorded through touch.” Make branding editable.
Provide an owner workspace for collecting and preparing entries and a public, read-only website where people can see the textures I have chosen to publish. No visitor upload or editing system is needed. Online hosting is part of the intended outcome, not just a local demo. Use the existing project's hosting workflow when configured; otherwise prepare a suitable deployable build and document the remaining hosting prerequisite. Complete the working preview before deployment. Publish only approved public entries, using existing authorization where available; never expose the entire capture folder automatically.
If this environment has Sites tools and the task is creating this website, follow the Sites building and hosting skills. Otherwise use the repository's established provider and deployment configuration. Do not invent a live URL or claim deployment when only a build exists.
Implement this actual pipeline:
1. Select a captured patch, stitched atlas, or compatible imported mesh/height field.
2. Review the valid texture area and preserve the original source.
3. Generate an ink impression from the relief using the print engine below.
4. Save it to my dictionary as a draft with a persistent ID, editable title, tags, and story.
5. Generate preview images and available material/print derivatives.
6. Preview the exact public entry and select which assets visitors can view or download.
7. Publish or update that entry, keeping its public address stable.
An entry's identity is the collected specimen, not one particular rendering. Let one specimen have several impressions, palettes, and relief interpretations without creating duplicate specimens. Preserve the link between each derivative and its source/version/settings. Provide draft, published, and archived states, including unpublish and later republish.
Use a valid height field as the primary input. For mesh inputs, reuse the Print Studio surface projection/extraction path where appropriate. If only raw tactile RGB exists, permit an image-based artistic impression clearly identified as such, without claiming height-based contact simulation.
Build an ink-coverage model with explicit controls for:
- Contact threshold / virtual printing pressure: higher pressure includes lower parts of the surface.
- Ink amount / opacity and pigment color.
- Softness of the contact transition.
- Positive relief versus deliberately inverted relief.
- Dry-brush breakup, mild ink spread, and edge softness.
- Paper color and an optional subtle paper-grain effect.
- Orientation, crop, margin, and scale bar when physical calibration exists.
For a first deterministic model, let ink coverage be a smooth threshold of height: coverage = sigmoid((h - threshold(pressure)) / softness), evaluated only over the valid mask. Threshold decreases as pressure increases. Define behavior for constant-height fields and zero softness, handle missing data, and keep the height polarity consistent. Compute the pigment/paper composite from this coverage; keep coverage available as a separate transparent-mask export.
These are artist-controlled printing effects, not measured ink-transfer physics. Do not suggest that a force slider recreates the actual force used during scanning. Procedural paper grain and added ink artifacts must remain removable layers. They must not enter wrinkle measurements, calibrated relief, or printable mesh generation.
Preserve natural grain and directional character. The default impression should come from the scan, not generated replacement textures. Do not use generative AI to invent surface detail by default. If the project already has a generative variation feature, isolate it as an explicitly labeled derivative.
Offer three clear presentation styles: clean specimen impression, ink-on-paper print, and relief-shaded view. The user should be able to compare them with the original capture. Provide a slider to move through printing pressure, making the surface's peaks and valleys progressively visible. Export a high-resolution print PNG, transparent ink mask, and optionally SVG only if genuinely vectorized with a declared simplification tolerance. Do not relabel a raster as vector.
Keep measured scale separate from decorative page size. An exported print should record both source patch dimensions and reproduction magnification. Uncalibrated entries use an authored display size without a fabricated physical scale bar.
Build a visually considered collection website with warm paper tones, dark ink, generous margins, and readable typography. Center the collected impressions; avoid a generic analytics dashboard or stock imagery. Use restrained movement and respect reduced-motion preferences.
The home page should introduce the collection briefly and show a fast-loading grid of specimen prints. Each card shows the impression, title, specimen number, and material/category. Support search, filtering by material/body region/tags, and sorting by title or collection date. Allow switching between print thumbnails and material-sphere thumbnails without loading a live 3D scene in every card.
Provide individual stable entry pages containing:
- Large zoomable ink impression, with clear indication of measured versus artistic imagery.
- Name, specimen number, material/category, collection date if recorded, and a short owner-written note.
- Optional body-region label, approximate collection location if intentionally provided, and calibrated source dimensions if known.
- Tabs or simple view switches for Print, Surface, Material Sphere, and Physical Relief, showing only available assets.
- Interactive lighting and rotation for a lazily loaded sphere or mesh; a static fallback on unsupported devices.
- Impression controls that visitors can play with locally, plus a reset button. Visitor adjustments do not modify the saved specimen.
- Related specimens and a link back to the collection.
- Downloads only for owner-enabled assets, with clear license/attribution information.
For a physical relief, distinguish the downloadable 3D model or rendered preview from a photograph of an actual printed object. Never claim the item has been fabricated without a supplied photograph or owner-entered status.
Add a Compare view for two to four specimens. Synchronize scale, ink settings, or lighting only when selected and applicable. Support paired before/after moisturizer entries as a collection story, keeping capture conditions and measurements distinct from artistic renders. Do not automatically describe one as healthier or more hydrated based on texture alone.
Include an About / Process page that explains collecting a tactile impression, deriving digital ink, and making relief models in accessible language. Credit the gyotaku inspiration respectfully and cite technical research already used by Material Studio. Explain that material colors and shader roughness may be authored rather than measured.
Reuse the app's existing authentication and storage. If using static publication, keep editing local/private and export only an allowlisted public dataset; do not add an insecure public admin form. A client-side hidden route or hard-coded password is not authentication. Public visitors should need no account.
Add a Specimen record with a persistent ID and stable slug, title, optional scientific/common name where entered by me, category, tags, body region if relevant, collection timestamp, optional public location, notes, source patch/atlas ID, calibration status, physical dimensions, and publication state. Do not infer location, subject identity, or missing provenance.
Add ImpressionVariant records with source version, print-engine settings, reproducible procedural seed, pixel size, print size, scale/magnification, palette, thumbnail, and exports. Reference existing MaterialAsset and PrintProject records for spheres and printable models instead of duplicating their data.
Keep original body captures and identifying metadata private by default. Let me publish an abstract print or material sphere without publishing raw images. Explicitly build public manifests from approved fields and assets; do not place private originals in a public bucket and merely omit links. Protect draft assets and private API routes as well as the interface.
Allow selecting a cover impression, editing the story, ordering related samples, previewing changes, and publishing a revision. Preserve prior derivative settings so I can reproduce an earlier print. Do not overwrite source captures during edits. Label synthetic demonstration entries unmistakably, and omit them from production publication unless selected.
Reuse the existing stack. If a new frontend is necessary, React/TypeScript with the existing Three.js viewer is a reasonable default. Choose static generation or a lightweight content API according to the repository. Keep image/mesh processing out of public page requests.
Generate responsive thumbnails and optimized display images while retaining separate high-resolution masters. Lazy-load detail images and 3D viewers, dispose GPU resources, and cap interactive texture sizes. Keep public asset URLs versioned so updates invalidate caches without changing entry URLs. Make direct entry links and browser refresh work after hosting, including routes served from subdirectories if applicable.
Implement keyboard navigation, meaningful image descriptions, touch zoom controls, visible focus, and usable mobile layouts. Include page titles, descriptions, and public social-preview images without exposing private metadata. Respect any user-selected download restrictions, while explaining in owner documentation that publicly displayed images can still be saved by viewers.
Export an exhibition bundle containing selected public prints, approved captions, a collection manifest, and a self-contained site build or documented deployment bundle. Keep any optional print-sheet PDF or contact sheet as a separate export; the primary deliverable is the browsable online dictionary.
Demonstrate a complete entry using a real provided capture when available. Otherwise use a deterministic synthetic fixture clearly labeled as a demo, and make import of my real capture work without code changes.
Verify that increasing virtual pressure changes coverage predictably, flat and masked surfaces behave correctly, inversion is deliberate, and regeneration with the same inputs/settings produces the same impression. Confirm that procedural ink effects do not modify source geometry or measurements.
Test the end-to-end flow: import -> impression -> draft -> public preview -> publish/export -> stable entry page. Verify filters, mobile navigation, lazy 3D loading, asset downloads, update/unpublish behavior, and direct page access. Confirm that private source files and unpublished records are absent from public builds, assets, and API responses.
Deliver implemented code, a runnable preview, build/deployment instructions, the specimen schema, sample content, and a concise verification summary. If deployed, supply the real public URL and what was published. If deployment is blocked, identify the exact missing prerequisite and provide the completed deployable result. Preserve the existing Material Studio functions and document how its scans become dictionary entries